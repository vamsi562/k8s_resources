apiVersion: v1
kind: Pod
metadata:
  name: pod-lb
  namespace: roboshop
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: LoadBalancer
spec:
  containers:
    - name: nginx
      image: nginx
