apiVersion: v1
kind: Service
metadata:
  name: pod-secret
  namespace: roboshop
spec:
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
    - protocol: TCP
      port: 80 #servicePort
      targetPort: 80 #containerPort
